# Master server for FO1

Building:

```console
$ cargo build --release
```

A sample configuration file will be generated in the `target` directory. Place it next to the server executable,
configure and run.